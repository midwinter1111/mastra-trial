# VenueMap Marché — 環境変数サンプル
# このファイルをコピーして .env.local を作成してください
# .env.local は .gitignore に含まれており、リポジトリにコミットされません

# Anthropic API キー（Claude AI 機能を使用する場合に設定）
# 未設定の場合、AI 機能のボタンが disabled になります
# それ以外の全機能はAPIキーなしで動作します
VITE_ANTHROPIC_API_KEY=
